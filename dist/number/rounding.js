"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.clamp = exports.roundTo = exports.truncate = exports.toPrecision = exports.toFixed = exports.floor = exports.ceil = exports.round = void 0;
const round = (num) => Math.round(num);
exports.round = round;
const ceil = (num) => Math.ceil(num);
exports.ceil = ceil;
const floor = (num) => Math.floor(num);
exports.floor = floor;
const toFixed = (num, decimals) => num.toFixed(decimals);
exports.toFixed = toFixed;
const toPrecision = (num, precision) => num.toPrecision(precision);
exports.toPrecision = toPrecision;
const truncate = (num) => Math.trunc(num);
exports.truncate = truncate;
const roundTo = (num, decimals) => Math.round(num * Math.pow(10, decimals)) / Math.pow(10, decimals);
exports.roundTo = roundTo;
const clamp = (num, min, max) => Math.max(min, Math.min(max, num));
exports.clamp = clamp;
